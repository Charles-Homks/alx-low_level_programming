#include "main.h"

/**
 * mul - function that multiplies two integers and returns the result
 *
 * @a: collect integer variable for addition
 * @b: collect integer variable for addition
 *
 * Return: multiplication value
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
