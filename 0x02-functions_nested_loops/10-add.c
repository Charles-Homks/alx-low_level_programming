#include "main.h"

/**
 * add - function that adds two integers and returns the result
 *
 * @i: collect integer variable for addition
 * @j: collect integer variable for addition
 *
 * Return: add value
 */
int add(int i, int j)
{
	int add;

	add = i + j;
	return (add);
}
