#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Dora[] = "\"Programming is like building a multilingual puzzle";

	puts(Dora);
	return (0);
}
