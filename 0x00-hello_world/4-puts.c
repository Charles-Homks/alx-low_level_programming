#include <stdio.h>

/**
 * main - Entry point
 *
 * puts: use to write a line or string to the out stdout
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
